{
  "name": "cooldigs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "cross-per-env": "^1.0.5",
    "dotenv": "^16.0.3",
    "js-tokens": "^4.0.0",
    "loose-envify": "^1.4.0",
    "object-assign": "^4.1.1",
    "per-env": "^1.0.2",
    "prop-types": "^15.8.1",
    "react": "^17.0.2",
    "react-is": "^16.13.1"
  },
  "scripts": {
    "render-postbuild": "npm run build --prefix frontend",
    "install": "npm --prefix backend install backend && npm --prefix frontend install frontend",
    "dev:backend": "npm install --prefix backend start",
    "dev:frontend": "npm install --prefix frontend start",
    "sequelize": "npm run --prefix backend sequelize",
    "sequelize-cli": "npm run --prefix backend sequelize-cli",
    // "build": "npm run build --prefix backend",
    "build": "npm install && npm run render-postbuild && npm run build && npm run sequelize --prefix backend db:migrate && npm run sequelize --prefix backend db:seed:all",
    "start": "npm start --prefix backend"

  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Kristen-Reid/CoolDigs.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Kristen-Reid/CoolDigs/issues"
  },
  "homepage": "https://github.com/Kristen-Reid/CoolDigs#readme",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "dotenv-webpack": "^8.0.1"
  }
}
